name: entity_data_lakehouse
version: '1.0.0'
profile: entity_data_lakehouse

model-paths: ["models"]
test-paths: ["tests"]

models:
  entity_data_lakehouse:
    gold:
      +materialized: table
      +schema: analytics

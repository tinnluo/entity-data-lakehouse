select
    asset_id,
    asset_name,
    asset_country,
    asset_sector,
    capacity_mw,
    latitude,
    longitude,
    altitude_avg_m,
    territorial_type,
    economic_level,
    gdp_tier,
    solar_irradiance_kwh_m2_yr,
    wind_speed_avg_ms,
    regulatory_stability_score,
    typical_lifespan_years,
    predicted_lifecycle_stage,
    lifecycle_stage_confidence,
    estimated_retirement_year,
    estimated_commissioning_year,
    predicted_remaining_years,
    predicted_capacity_factor_pct,
    model_version
from {{ source('lakehouse', 'ml_asset_lifecycle_predictions') }}
